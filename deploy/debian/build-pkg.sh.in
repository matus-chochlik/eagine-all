#!/bin/bash
#
pkg_dir=$(realpath "@PKG_DIR@")

mkdir -p "${pkg_dir}/DEBIAN"

# ------------------------------------------------------------------------------
mkdir -p "${pkg_dir}/DEBIAN"
cat << EOD > "${pkg_dir}/DEBIAN/control"
Package: eagine-all-dev
Version: @PKG_VERSION@
Architecture: @PKG_ARCH@
Installed-Size: 0
Depends: eagine-core-dev,eagine-ecs-dev,eagine-sslplus-dev,eagine-msgbus-dev,eagine-shapes-dev,eagine-eglplus-dev,eagine-oglplus-dev,eagine-oalplus-dev,eagine-app-dev
Maintainer: Matus Chochlik <chochlik@gmail.com>
Description: Metapackage installing all eagine sub-packages.
EOD
